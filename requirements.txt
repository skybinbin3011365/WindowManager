# 窗口管理器项目依赖
# ====================
# 运行时依赖（生产环境必须）

# 核心界面框架
PySide6>=6.11.0

# 进程管理和系统监控
psutil>=7.0.0

# Windows API访问（包含win32gui, win32con等）
pywin32>=311

# 全局热键支持（使用pynput实现，支持鼠标+键盘组合键）
pynput>=1.7.6
